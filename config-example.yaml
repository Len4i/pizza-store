db:
  host: localhost
  port: 3306
  user: root
  password: root_password!
  db_name: pizza_store
  type: sqlite
  storage_path: /tmp/sqlit.db
http_server:
  address: :8080
  read_header_timeout: 4s
  idle_timeout: 60s
graceful_shutdown_timeout: 30s
log_level: 0 # 0: info, -4: debug
